import React from 'react';
import ReactDOM from 'react-dom/client';
import Aplicacion from './Aplicacion';
import './estilos/estilos.css'; // Archivo CSS para estilos

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Aplicacion />
  </React.StrictMode>
);
